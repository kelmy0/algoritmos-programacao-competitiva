export interface Algorithm {
	publicId: string;
	slug: string;
	name: string;
	category: string;
	content: string;
	difficulty: string;
	status: string;
	authorId: string;
	createdAt: Date;
	updatedAt: Date;
}
